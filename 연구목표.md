# 연구 목표 및 개발 계획

## 1. 최종 연구 목표

본 연구의 목표는 pretrained Transformer 및 생성 모델을 재학습 없이 hardware-friendly approximate inference graph로 변환하여, GPU 중심 추론 대비 전력 효율을 크게 개선하는 범용 operator-level conversion framework를 제안하는 것이다.

구체적으로는 기존 ANN 모델의 구조와 가중치를 유지하면서, 추론 과정에서 큰 전력과 면적을 차지하는 FP32 곱셈, 비선형 함수, 정규화 연산을 하드웨어 친화적인 근사 연산자로 치환하는 것을 목표로 한다. 이 프레임워크는 Transformer 계열 모델을 우선 대상으로 하며, ViT, GPT 계열 언어 모델, DiT 계열 생성 모델처럼 attention, MLP, normalization, activation 연산을 공통적으로 포함하는 pretrained 모델에 적용하는 것을 1차 목표로 한다.

본 연구는 전통적인 timestep 기반 SNN 변환을 그대로 따르기보다, SNN의 event-driven 및 spike-bit 표현의 장점을 arithmetic level에서 재구성한다. 즉, 내부 membrane dynamics를 반복적으로 시뮬레이션하는 방식이 아니라, exponent-guided bit-slice encoding, Mitchell/log-domain multiplication, S-PLA nonlinear approximation을 결합하여 FPGA-friendly inference graph를 구성한다.

## 2. 연구 배경 및 문제 정의

기존 training-free ANN-to-SNN conversion 연구는 pretrained Transformer를 별도 재학습 없이 SNN으로 변환할 수 있다는 가능성을 보여주었다. 특히 MBE neuron 기반 방식은 Transformer 내부의 GELU, Softmax, LayerNorm 등 비선형 연산을 SNN 도메인에서 근사하려는 중요한 시도이다.

그러나 기존 SNN 기반 접근에는 다음 문제가 남아 있다.

- 여러 timestep에 걸친 neuron dynamics 때문에 추론 latency가 증가한다.
- membrane potential update, decay, reset, basis combination 과정에서 지속적인 floating-point addition이 발생할 수 있다.
- FP32 곱셈을 줄이더라도 timestep-wise FP accumulation이 남으면 실제 전력 이득이 제한된다.
- GPU에서 빠르게 실행되는 dense matmul을 Python/PyTorch 수준에서 spike simulation으로 대체하면 추론 시간이 극단적으로 증가한다.
- 기존 SNN 변환의 energy claim은 실제 hardware dataflow, accumulator, memory movement 비용을 충분히 반영하지 못할 수 있다.

따라서 본 연구는 "SNN을 더 생물학적으로 모사하는 것"이 아니라, pretrained Transformer의 핵심 연산을 hardware-friendly approximate operator로 치환하여 edge hardware에서 저전력 추론을 가능하게 하는 방향으로 재정의한다.

## 3. 핵심 아이디어

### 3.1 Exponent-Guided Binary Fraction Encoding

현재 프로젝트의 IEEE754 기반 encoding은 Binary Fraction Encoding(BFE)의 확장형으로 볼 수 있다. 순수 BFE는 값을 다음과 같이 표현한다.

```text
x ~= sum_t s_t * 2^-t
```

하지만 Transformer activation은 layer별 dynamic range가 크므로 고정 범위 BFE만으로는 충분하지 않다. 이에 본 연구에서는 sign과 exponent를 side channel로 유지하고, mantissa를 spike-bit stream으로 표현한다.

```text
x ~= sign(x) * 2^e * sum_t s_t * 2^(-t+1)
```

이 방식은 timestep을 neuron dynamics가 아니라 mantissa bit-plane 또는 bit-serial precision step으로 해석한다. 따라서 기존 SNN의 temporal latency 문제와 구분된다.

### 3.2 Mitchell C-2 / Log-Domain Multiplication

Transformer의 Linear projection, MLP projection, QK^T, AV matmul은 대부분의 연산량과 전력을 차지한다. 본 연구는 FP32 multiplier를 Mitchell C-2 logarithmic multiplier 또는 보정된 log-domain multiplier로 대체한다.

현재 구현은 mantissa 구간별 4x4 LUT correction을 사용하여 multiplication을 근사한다. 이는 dense operator의 functional approximation을 검증하기 위한 1차 구현이다.

향후에는 layer별 activation 분포를 이용한 calibration LUT, signed error compensation, exponent bucket 기반 accumulation을 추가하여 정확도와 hardware efficiency를 개선한다.

### 3.3 S-PLA: Spike-Prefix Routed Nonlinear Approximation

S-PLA는 GELU, Tanh, Sigmoid, Softmax, LayerNorm 등의 비선형 연산을 PWL 방식으로 근사한다. 핵심은 mantissa spike-bit prefix와 exponent 정보를 이용하여 segment를 선택하고, 선택된 segment의 slope/intercept로 nonlinear output을 계산하는 것이다.

S-PLA는 단순 activation approximation이 아니라, bit-slice representation 위에서 nonlinear operator를 처리하기 위한 prefix-routed operator framework로 정의한다.

### 3.4 Hardware-Friendly Approximate Inference Graph

최종 목표는 PyTorch에서 느리게 approximate operation을 에뮬레이션하는 것이 아니라, 다음과 같은 hardware-native dataflow를 설계하는 것이다.

```text
Pretrained Transformer
-> operator-level graph conversion
-> exponent-guided bit representation
-> Mitchell/log-domain product unit
-> integer or block-floating accumulator
-> S-PLA nonlinear operators
-> FPGA-friendly streaming inference graph
```

이때 가장 중요한 수정 사항은 FP32 accumulator를 제거하고, integer, fixed-point, 또는 block floating-point accumulator로 대체하는 것이다.

## 4. 현재 프로젝트에서 구현된 사항

현재 프로젝트는 다음 요소들을 이미 포함하고 있다.

- IEEE754 기반 exponent-guided bit-slice encoder
- Mitchell C-2 approximate multiplier
- Mitchell C-2 기반 Linear, Conv1D, attention matmul approximation
- S-PLA 기반 GELU/Tanh/Sigmoid approximation
- S-PLA 기반 LayerNorm 및 Softmax approximation prototype
- MNIST MLP 변환 및 정확도/energy estimate 실험
- GPT-2 변환 및 Wikitext perplexity 평가 prototype
- ViT 변환 및 Imagenette classification 평가 prototype
- VAE 및 DiT 생성 모델 변환 prototype
- FPGA/HLS용 primitive 수준 구현
  - IEEE754 encoder
  - Mitchell C-2 multiplier
  - S-PLA activation

현재 구현의 가장 중요한 의미는 pretrained 또는 trained model의 일부 operator를 approximate operator로 치환해도 output quality가 크게 무너지지 않을 가능성을 보여준다는 점이다.

## 5. 현재 구현의 한계

현재 코드는 연구 방향을 검증하기 위한 functional simulator에 가깝다. 따라서 다음 한계를 명확히 인식해야 한다.

- PyTorch 구현은 실제 hardware latency를 대표하지 않는다.
- Mitchell matmul이 GPU의 optimized matmul kernel이 아니라 elementwise/broadcast/chunk loop 방식으로 시뮬레이션되어 매우 느리다.
- GPT-2 및 ViT 일부 baseline metric이 코드에 hard-coded되어 있어, 논문용 결과로 쓰기 전 동일 run에서 baseline과 converted model을 함께 재측정해야 한다.
- energy saving 결과는 실제 전력 측정이 아니라 operator-level analytical estimate이다.
- exponent routing, LUT access, memory movement, accumulator, buffering 비용이 현재 energy model에 충분히 반영되어 있지 않다.
- LayerNorm, Softmax, 일부 S-PLA 연산에는 아직 floating-point operation이 남아 있다.
- FPGA/HLS 구현은 primitive 수준이며, end-to-end Transformer block accelerator는 아직 구현되지 않았다.
- HLS S-PLA activation과 Python S-PLA activation의 approximation fidelity가 완전히 일치하지 않는다.
- DiT random-weight mode 결과는 실제 pretrained generative quality 검증으로 보기 어렵다.

따라서 현재 결과는 "최종 hardware-efficient inference system 검증"이 아니라 "operator substitution의 functional viability 및 theoretical energy potential 검증"으로 해석해야 한다.

## 6. 수정해야 할 사항

### 6.1 연구 claim 수정

기존 표현 중 다음 claim은 줄이거나 재정의해야 한다.

- fully spiking
- universal for any model
- zero-cost encoding
- no accuracy loss
- FPGA에서 즉시 GPU 대체
- 실제 전력 68% 절감

대신 다음 표현을 사용한다.

- training-free operator-level conversion
- Transformer-oriented approximate inference graph
- exponent-guided bit-slice representation
- near-lossless functional approximation
- FPGA-friendly dataflow
- analytical compute energy reduction
- hardware-aware projected efficiency

### 6.2 Baseline 재측정

GPT-2, ViT, DiT 실험은 baseline과 converted model을 같은 조건에서 재측정해야 한다.

GPT-2:

- 같은 tokenizer
- 같은 dataset split
- 같은 stride, max length, num samples
- baseline PPL과 converted PPL을 동일 script에서 계산

ViT:

- 같은 validation subset
- seed 고정
- baseline Top-1/Top-5와 converted Top-1/Top-5를 동일 run에서 계산

DiT:

- pretrained model 기준 여러 seed 평가
- latent MSE
- pixel MSE
- CLIP score 또는 FID subset
- 생성 이미지 예시
- diffusion step 수 고정

### 6.3 Accumulator 재설계

가장 중요한 알고리즘 수정은 FP32 accumulation 제거이다. 현재 곱셈을 Mitchell 방식으로 근사하더라도 sum이 FP32 add로 남으면 전력 이득이 줄어든다.

권장 구조:

```text
Mitchell/log-domain product
-> sign/exponent/mantissa contribution
-> exponent bucket alignment
-> integer mantissa accumulation
-> block floating-point normalization
```

이 구조를 통해 multiplication뿐 아니라 accumulation까지 hardware-friendly하게 만들어야 한다.

### 6.4 Energy Model 개선

현재 energy estimate에는 다음 항목을 추가해야 한다.

- exponent/sign routing cost
- LUT read cost
- integer/fixed-point accumulator cost
- block-float normalization cost
- memory access cost
- activation buffer cost
- tile load/store cost
- FPGA BRAM/URAM/HBM access cost
- control logic overhead
- bit-serial latency cost

최종적으로는 analytical estimate와 HLS synthesis report를 함께 제시해야 한다.

### 6.5 HLS Primitive 정합성 개선

Python implementation과 HLS implementation이 동일한 수치 결과를 내도록 정합성을 맞춰야 한다.

우선 대상:

- IEEE754 encoder decode error
- Mitchell multiplier relative error
- S-PLA GELU approximation error
- S-PLA Softmax KL divergence
- LayerNorm output cosine similarity / MSE

## 7. 추가해야 할 실험

### 7.1 Operator-Level Error Analysis

각 operator별 approximation error를 독립적으로 측정한다.

- Mitchell multiplication MAE, MSE, relative error, p99 error
- Linear output MSE/cosine similarity
- Attention score MSE
- Attention probability KL divergence
- AV output MSE
- GELU MaxAE/MSE
- LayerNorm output MSE
- Softmax KL divergence 및 top-k overlap

### 7.2 Ablation Study

각 구성 요소가 정확도와 energy에 미치는 영향을 분리한다.

- Mitchell only
- S-PLA activation only
- S-PLA LayerNorm only
- S-PLA Softmax only
- Mitchell attention matmul only
- Full conversion
- FP32 accumulator vs block-float accumulator
- prefix_k 변화
- timesteps 변화
- LUT size 변화

### 7.3 Transformer Block-Level Verification

전체 모델보다 먼저 단일 Transformer block에서 검증한다.

- baseline block output
- converted block output
- residual path 이후 output drift
- multi-block accumulation drift
- activation distribution shift

### 7.4 Hardware-Oriented Evaluation

FPGA 전체 모델 구현 전에 primitive와 tile 단위로 검증한다.

- Mitchell matmul tile HLS synthesis
- S-PLA GELU HLS synthesis
- EG-BFE encoder HLS synthesis
- block-floating accumulator HLS synthesis
- one Transformer block streaming dataflow prototype

측정 항목:

- LUT
- FF
- DSP
- BRAM/URAM
- latency
- initiation interval
- frequency
- estimated power
- throughput

## 8. 연구 방향

본 연구는 "SNN timestep을 줄이는 연구"가 아니라, "SNN conversion의 목적이었던 event-driven low-power computation을 arithmetic graph 수준에서 재구성하는 연구"로 정의한다.

기존 MBE 기반 SNN Transformer와의 차별점은 다음과 같다.

```text
MBE-based conversion:
  multi-basis neuron dynamics로 nonlinear operation을 근사
  timestep-wise state update와 basis combination이 남음

Proposed framework:
  neuron dynamics를 제거하고 bit-slice arithmetic으로 재정의
  exponent-guided representation으로 dynamic range 보존
  Mitchell/log-domain multiplier로 dense multiplication 근사
  S-PLA prefix routing으로 nonlinear operator 근사
  FPGA-friendly accumulator/dataflow로 전력 효율 개선
```

따라서 본 연구의 핵심 contribution은 다음과 같이 정리한다.

1. Pretrained Transformer를 재학습 없이 approximate inference graph로 변환하는 operator-level framework
2. BFE의 dynamic range 한계를 확장한 exponent-guided bit-slice representation
3. Dense projection 및 attention matmul을 위한 Mitchell/log-domain multiplication
4. Transformer nonlinear operator를 위한 S-PLA prefix-routed PWL approximation
5. FPGA-friendly execution을 위한 integer/block-floating accumulation 방향 제시
6. Accuracy retention과 analytical/hardware-projected energy saving의 체계적 평가

## 9. 단계별 개발 계획

### Phase 1: 실험 코드 정리 및 재현성 확보

- README 결과와 저장된 plot 결과 수치 일치
- GPT-2 baseline hard-code 제거
- ViT baseline hard-code 제거
- seed, dataset subset, num_samples 고정
- 실험 결과를 JSON/CSV로 저장
- plot은 저장된 metric 파일에서 생성

### Phase 2: Functional Approximation 검증 강화

- operator-level error analysis 추가
- ablation study 추가
- Transformer block-level output drift 분석
- DiT pretrained mode 다중 seed 평가

### Phase 3: Accumulator 및 Energy Model 개선

- FP32 accumulator를 block-floating 또는 fixed-point accumulator로 대체하는 prototype 구현
- exponent bucket accumulation 실험
- memory-aware energy model 추가
- 현재 68% energy saving 결과를 "operator-level estimate"로 재정의

### Phase 4: Hardware Primitive 검증

- HLS primitive와 Python primitive 수치 정합성 맞추기
- Mitchell matmul tile 구현
- S-PLA activation/Softmax/LayerNorm primitive 개선
- block-float accumulator HLS 구현
- primitive별 latency/power/resource report 정리

### Phase 5: Transformer Block Accelerator Prototype

- 단일 Transformer block의 streaming dataflow 설계
- projection, attention, MLP, LayerNorm, activation의 operator pipeline 구성
- FPGA synthesis 또는 cycle-accurate estimate 수행
- GPU 대비 latency가 아니라 energy-per-token 또는 energy-per-sample 중심으로 비교

### Phase 6: 논문용 결과 정리

- ViT, GPT-2, DiT를 Transformer 대표 task로 선정
- accuracy/quality retention 표준 지표 제시
- analytical energy estimate와 HLS-based projected energy를 구분
- PyTorch simulation latency는 hardware latency와 분리
- 한계와 future work 명시

## 10. 현재 결과의 올바른 해석

현재 프로젝트에서 얻은 정확도 유지 및 약 68% energy saving 결과는 연구 가능성을 보여주는 중요한 preliminary result이다. 그러나 이는 실제 FPGA 전력 실측이 아니라, operator-level analytical energy model에 기반한 추정치이다.

따라서 현재 결과는 다음과 같이 표현해야 한다.

```text
The proposed operator substitution preserves model output quality in preliminary functional simulations.
Under an operator-level analytical energy model, the proposed framework estimates up to approximately 68% dynamic compute energy reduction.
```

즉, 현재 결과는 잘못된 결과라기보다 해석 범위를 명확히 해야 하는 결과이다. 정확도 유지 가능성은 긍정적인 신호이며, energy saving은 hardware-aware 검증으로 확장해야 할 theoretical projection이다.

## 11. 최종 정리

본 연구는 pretrained Transformer 및 생성 모델을 edge hardware에서 저전력으로 실행하기 위한 training-free approximate operator conversion framework를 목표로 한다. 현재 구현은 이 방향의 functional feasibility를 보여주는 초기 prototype이며, 앞으로는 baseline 재측정, accumulator 재설계, memory-aware energy model, FPGA primitive synthesis를 통해 연구 claim을 강화해야 한다.

가장 중요한 연구 방향은 다음과 같다.

```text
SNN의 내부 동역학을 더 잘 시뮬레이션하는 것이 아니라,
SNN이 목표로 했던 event-driven low-power computation을
bit-slice approximate arithmetic과 FPGA-friendly dataflow로 재구성한다.
```

